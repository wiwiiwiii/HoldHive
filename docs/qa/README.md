# HoldHive QA Notes

Use this folder when validating `qa`, preparing a release PR to `main`, or checking the `1.0.0` demo snapshot.

Current status:

- `1.0.0` is tagged from `main`.
- New fixes and documentation updates still start from `qa`.
- Frontend integration for holdings CRUD, market quotes, fund lookthrough, portfolio exposure, data mode, and AI insights is already implemented; the notes below are now regression references rather than pending tasks.

- `acceptance-checklist.md`: end-to-end validation checklist.
- `api-test-cases.md`: curl/Postman-ready backend API test cases.
- `demo-seed-data.md`: default portfolio seed data and smoke checks for frontend/backend sync.
- `frontend-integration-notes.md`: implemented holdings update contract and frontend regression notes.
- `frontend-market-integration-notes.md`: implemented market quotes, fund lookthrough, exposure, and AI-analysis frontend notes.
- `defect-log.md`: defects found during validation and regression status.
